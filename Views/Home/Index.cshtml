 @model List<Alumnos>

@if (Model != null && Model.Count > 0)
{
    <h4>Existen valores en la tabla Alumnos</h4>
}
else
{
    <h4>No existen valores en la tabla Alumnos</h4>
}

 

<h1>Esta es la pagina pricipal de mi controlador alumnos</h1>
   
 
